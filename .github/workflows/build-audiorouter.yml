name: Build AudioRouter (cross-platform)

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build on ${{ matrix.os }} (${{ matrix.arch }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            arch: x64
            cmake-args: ''
            artifact: AudioRouter-linux-x64
          - os: ubuntu-24.04-arm
            arch: arm64
            cmake-args: ''
            artifact: AudioRouter-linux-arm64
          - os: windows-latest
            arch: x64
            cmake-args: '-A x64'
            artifact: AudioRouter-windows-x64.exe
          - os: windows-latest
            arch: x86
            cmake-args: '-A Win32'
            artifact: AudioRouter-windows-x86.exe
          - os: macos-latest
            arch: arm64
            cmake-args: '-DCMAKE_OSX_ARCHITECTURES=arm64'
            artifact: AudioRouter-macos-arm64
          - os: macos-latest
            arch: x64
            cmake-args: '-DCMAKE_OSX_ARCHITECTURES=x86_64'
            artifact: AudioRouter-macos-x64

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install Linux deps
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libasound2-dev libpulse-dev

      - name: Configure CMake
        run: cmake -B build -S AudioRouter -DCMAKE_BUILD_TYPE=Release ${{ matrix.cmake-args }}

      - name: Build
        run: cmake --build build --config Release --parallel

      - name: Stage artifact
        shell: bash
        run: |
          mkdir -p staging
          if [ "${{ runner.os }}" = "Windows" ]; then
            if [ -f build/Release/AudioRouter.exe ]; then
              cp build/Release/AudioRouter.exe "staging/${{ matrix.artifact }}"
            else
              cp build/AudioRouter.exe "staging/${{ matrix.artifact }}"
            fi
          elif [ "${{ runner.os }}" = "macOS" ]; then
            cp build/AudioRouter "staging/${{ matrix.artifact }}"
          else
            cp build/AudioRouter "staging/${{ matrix.artifact }}"
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v5
        with:
          name: ${{ matrix.artifact }}
          path: staging/*
          if-no-files-found: error
          retention-days: 30

  release:
    name: Attach to Release (on tag)
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v5
        with:
          path: artifacts
          merge-multiple: true

      - name: Create or update release
        uses: softprops/action-gh-release@v3
        with:
          files: artifacts/*
          name: sndcpy++ ${{ github.ref_name }}
          body: |
            ## sndcpy++ ${{ github.ref_name }}

            Android 设备音频/视频转发与管理的桌面应用，基于 PyQt6 + scrcpy + sndcpy.apk 构建。

            ### 主要功能

            - 音频转发（sndcpy.apk + AudioRouter）
            - 视频镜像（scrcpy）
            - 文件管理（上传/下载/浏览）
            - 屏幕录制
            - 设备状态实时监控
            - ADB 调试控制台

            ### v1.0.0 改进

            - 设备列表新增实时状态指示（音频/视频/录制）
            - 文件传输支持并发与进度反馈，修复 root 文件识别
            - 调试控制台 UI 优化（日志框可拖动、长设备名展示）
            - 引入 ruff / mypy / pytest 工程标准，测试覆盖 196 项
            - 修复代码审查发现的 14 项缺陷

            ### 下载

            | 平台 | sndcpy++ | AudioRouter |
            |------|----------|-------------|
            | Windows x64 | `sndcpy++-windows-x64.zip` | `AudioRouter-windows-x64.exe` |
            | Windows x86 | — | `AudioRouter-windows-x86.exe` |
            | macOS Apple Silicon | `sndcpy++-macos-arm64.zip` | `AudioRouter-macos-arm64` |
            | macOS Intel | `sndcpy++-macos-x64.zip` | `AudioRouter-macos-x64` |
            | Linux x64 | `sndcpy++-linux-x64.zip` | `AudioRouter-linux-x64` |
            | Linux ARM64 | — | `AudioRouter-linux-arm64` |

            > sndcpy++ 桌面端不提供 32 位包：上游 PyQt6 已停止发布 32 位 wheels，且 vendor 内置的 scrcpy 为 64 位二进制。AudioRouter 为 C++ 原生编译，仍提供 Windows x86 二进制。

            ### 使用方法

            1. 下载对应平台的 sndcpy++ 压缩包并解压
            2. 运行其中的可执行文件
            3. USB 连接 Android 设备（需开启 USB 调试）
            4. 在应用中选择设备并开始音频/视频转发
          generate_release_notes: false
