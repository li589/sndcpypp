# [OPEN] Debug Session: video-window-auth

## Symptoms
- 点击一键路由后，手机会弹出屏幕录制授权提示。
- 用户点击授权后，桌面端仍然没有出现视频窗口。
- 音频链路此前看起来基本可用，问题更集中在视频预览窗口未出现。

## Expected
- 用户在手机上点击录屏授权后，桌面端应出现 `scrcpy` 视频窗口。
- 如果窗口未出现，桌面端应能给出可定位的运行时信号，而不是只有“已启动”的泛化提示。

## User Evidence
- 当前仍能稳定触发手机侧录屏授权弹窗。
- 授权后也没有看到图像框。
- 上一轮已修复按钮不可点击、安装状态误判、以及音频参数重复问题。

## Hypotheses
1. `scrcpy` 进程实际已经很快退出，但当前日志只记录了“启动”阶段，没有把退出原因、退出码和时序完整暴露出来。
2. `scrcpy` 进程没有退出，而是因为 Windows 侧窗口创建参数或启动方式导致窗口未真正显示出来。
3. 录屏授权完成后，`scrcpy` 仍卡在设备侧初始化阶段，桌面端缺少对 `stdout/stderr` 关键信息的采集，误以为“已启动”。
4. 一键路由里视频和音频并发启动后，某个前置参数或额外参数与 `scrcpy` 组合发生冲突，导致视频窗口无法正常建立。
5. `scrcpy` 可执行文件路径或命令构造本身正确，但实际启动目录/子进程环境有问题，导致仅能拉起授权流程，不能完成桌面窗口展示。

## Plan
1. 先给视频路由链路补最小化埋点，覆盖命令构造、进程 PID、短时存活、退出码和 `stdout/stderr`。
2. 请用户复现一次，采集 `pre-fix` 运行时证据。
3. 基于日志确认到底是“快速退出”“窗口未显示”“设备侧卡住”还是“参数冲突”。
4. 只在证据明确后做最小修复，并再采 `post-fix` 证据对比。

## Evidence Summary
- 假设 1：排除为主因。`scrcpy` 多次通过了 2.5 秒存活检查，并不是一启动就立即退出。
- 假设 2：排除为主因。日志显示问题并非单纯“窗口被隐藏”，而是 `scrcpy` 在后续阶段发生了真实错误。
- 假设 3：已确认。运行时日志采到了 `stderr`，明确出现 `Demuxer 'audio': stream disabled due to connection error` / `Demuxer error`。
- 假设 4：已确认。当前视频命令未禁用 `scrcpy` 自带音频，而主程序又同时启动 `sndcpy` 音频路由，形成冲突。
- 假设 5：部分确认。另一次运行中采到了 `CreateProcessW() error 5`，说明 `scrcpy` 拉起子进程 `adb` 时还存在工作目录/可执行定位不稳定的问题。

## Fix Summary
- 视频命令默认追加 `--no-audio`，避免 `scrcpy` 自带音频与独立 `sndcpy` 音频路由发生冲突。
- 启动 `scrcpy` 时显式设置 `cwd` 为 `scrcpy.exe` 所在目录，减少 Windows 下子进程定位失败。
- 启动 `scrcpy` 时通过环境变量 `ADB` 显式绑定当前配置的 `adb.exe`，不再依赖 `scrcpy` 自己搜索裸 `adb`。
- 针对以上修复补齐回归测试，已通过针对性验证。
