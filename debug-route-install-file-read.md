# [OPEN] Debug Session: route-install-file-read

## Symptoms
- 安装 `sndcpy` 和一键启动路由点击后没有任何反应。
- 文件传输页读取目录经常第一次失败，刷新几次后才成功。

## Expected
- 安装 `sndcpy` 和一键启动路由后应至少出现明确的命令执行、进度变化或失败提示。
- 文件目录读取应稳定返回，不应随机出现 `WinError 5`。

## User Evidence
- 初始验证和设备枚举正常。
- `停止设备的流媒体路由` 命令已执行过，说明部分路由链存在可达性。
- 文件读取失败日志为 `adb.exe shell "ls -all "/sdcard/""`，随后重试又成功。

## Hypotheses
1. 安装和一键路由的 UI 触发链断了，按钮事件没有真正提交到 `CoreController` / service。
2. 安装和一键路由任务被前置校验或忙碌状态短路了，但没有补充失败日志，因此表现为“无反应”。
3. 文件读取失败不是 Android 权限问题，而是 Windows 侧 `subprocess.run()` 对错误拼接的带引号参数解释异常，导致偶发执行失败。
4. 文件页存在并发或状态竞争，请求与请求之间复用了不安全的命令/工作目录/缓存状态，导致首次失败、重试成功。
5. 路由相关播放器参数或命令模板在近期大改后不匹配，启动链在真正执行前就被静默拦截。

## Plan
1. 审查安装/一键路由的触发链，确认是否存在静默返回点。
2. 审查文件读取命令的构造、执行方式和并发控制。
3. 先添加最小化调试埋点，再请用户复现以收集运行时证据。

## Evidence Summary
- 假设 1：已确认。`安装 sndcpy` / `一键启动路由` 的入口埋点未出现，但 `RouteService.start_audio_route()` 的埋点出现，说明不是 service 整体不可用，而是这两个按钮未被实际触发。
- 假设 2：已确认。`apply_validation_result_ui()` 在写回 `are_paths_ready` 之前就调用了 `restore_validation_actions()`，导致按钮按旧状态恢复，可长期保持禁用。
- 假设 3：部分确认。文件读取失败时调试日志记录到同一命令可成功也可失败，失败时 `returncode=null`，说明问题发生在命令启动阶段，不是稳定的 Android 目录权限错误。
- 假设 4：高概率确认。文件页的符号链接探测绕过了统一的 ADB 执行通道，直接并发 `subprocess.run()`；与主列表请求交错时更容易触发偶发失败。
- 假设 5：已排除为主因。音频路由 service 能进入并生成播放器命令，不是“路由实现完全失效”。

## Fix Summary
- 修复验证结果回写顺序：按钮恢复逻辑现在直接使用“本次验证结果”控制可点击状态，不再读取旧的 `are_paths_ready`。
- 串行化文件页 ADB 请求：详细列表与符号链接探测共享同一把锁，减少跨页面切换时的并发 ADB 调用冲突。
- 对符号链接探测补齐与主 ADB 执行相近的 `cwd` 解析方式，降低 Windows 侧偶发 `WinError 5` 的触发概率。
